"use strict";

class Peticion{
    constructor(idPeticion, idUsuario, cola, nombreSolicitante, fechaCreacion, comentarios ){
        var idPeticion = idPeticion;
        var idUsuario = idUsuario;
        var cola = cola;
        var nombreSolicitante = nombreSolicitante;
        var fechaCreacion = fechaCreacion;
        var comentarios = comentario;
    }
}